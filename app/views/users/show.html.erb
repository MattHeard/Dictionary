<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Description:</strong>
  <% @user.description.split.each do |word| %>
    <%= link_to word, new_user_word_path(@user, word: word) %>
  <% end %>
</p>

<%= link_to 'Back', users_path %>
