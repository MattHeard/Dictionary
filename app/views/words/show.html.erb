<p id="notice"><%= notice %></p>

<p>
  <strong>Word:</strong>
  <%= @word.word %>
</p>

<p>
  <strong>Definition:</strong>
  <% @word.definition.split.each do |word| %>
    <%= link_to word, new_user_word_path(@user, new_word: word) %>
  <% end %>
</p>

<%= link_to 'Back', users_path %>
